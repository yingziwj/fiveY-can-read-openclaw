# 批处理验证参考

在这个仓库里，批量页面任务的默认验证顺序是：

1. 有内容或模板改动时，先运行 `npm run build`
2. 需要确认输出一致性时，再运行 `npm run check`
3. 涉及同步逻辑或源数据变化时，再考虑运行 `npm run sync`
4. 只有当任务明确要求时，才运行 `npm run verify-live`

如果验证失败：

- 先定位失败原因
- 优先修复与当前批任务直接相关的问题
- 不要因为一个可修复的小问题就提前结束整个批处理任务
